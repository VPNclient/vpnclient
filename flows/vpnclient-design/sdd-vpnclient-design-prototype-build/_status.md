# Status: sdd-vpnclient-design-prototype-build

## Current Phase

IMPLEMENTATION

## Phase Status

IN PROGRESS — Phases 0–6 all implemented and committed by user (`dbde8bc "cicd"`, 2026-07-26); Phase 0 l10n fix has now been reverted by content-import commits at least three times and re-applied by the agent five times — **confirmed committed and working on real CI** as of `8aac9ab2` (l10n error gone from CI log). A second, separate `analyze`-job failure was found on that same CI run (`flutter analyze` exits 1 on an info-level lint) and fixed in the working tree (uncommitted). Remaining: `flutter test` empty-`test/` gap (not yet exercised on real CI, was masked by the analyze failure until now), windows/ios/docker-build.yml still need real-CI confirmation, and the recurring l10n-revert risk is still unmitigated.

## Last Updated

2026-07-27 (new session) by Claude

## Blockers

- `flutter test` currently fails (exit 1: "does not appear to contain any test files") — `test/` is empty since boilerplate was deleted in Task 1.2 and no replacement test was ever added, but `build.yml`'s `analyze` job still runs `flutter test` unconditionally. This has not yet actually been exercised on real CI — a separate `flutter analyze` failure (see below, now fixed) was aborting the job before this step ran. Not yet decided whether to add a placeholder test, make the step non-fatal, or remove it — needs user decision.
- **Now fixed in working tree (uncommitted)**: real CI run on `8aac9ab2` showed the `analyze` job failing at the `flutter analyze` step itself — it exits 1 on any issue including `info`-level ones, and the repo has one pre-existing info lint (`ClearSubscriptions` naming in `lib/mock/vpnclient_engine_mock.dart:23`, deliberately PascalCase to mirror the real `vpnclient_engine_flutter` package's API). User chose the inline-ignore-comment fix (`// ignore: non_constant_identifier_names`) over `--no-fatal-infos` or renaming.
- **A newer commit (`b8576e1d`) reverted the ignore-comment fix again AND introduced 2 new genuine bugs**: `lib/design/widgets/unread_badge.dart` had a broken relative import (`'design/app_theme.dart'` instead of `'../app_theme.dart'`), causing cascading `AppColors` undefined-name/invalid-constant errors; `lib/pages/settings/action_button.dart` had a dead unused import of `design/app_theme.dart` left over from an earlier edit. All fixed in the working tree (uncommitted); `flutter analyze` verified clean (exit 0) after the fix.
- **Recurring, now confirmed on real CI, and user has since committed a fix**: `l10n.yaml` had `synthetic-package: true` in every commit from `2c782db` (2026-07-26) through `fe79c79` (2026-07-27 11:36) — confirmed broken on real CI (`analyze`/`build-macos` failed on `a33fe6d`). **User then committed `d998eb4 "fix"`** (2026-07-27 11:39) with the correct `l10n.yaml`/import paths — HEAD is now fixed. Since then, multiple further content-import commits (`0920eb2 "+7"` and others) have again reverted `l10n.yaml`/imports. Agent re-fixed the working tree again 2026-07-27 (new session) — this time explicitly without running any git commands, per user instruction — so the working tree currently has the correct fix but is neither staged nor committed. **User still needs to `git add`/commit `l10n.yaml` + the 7 import files.**

## Progress

- [x] Requirements drafted
- [x] Requirements approved (2026-07-26)
- [x] Specifications drafted
- [x] Specifications approved (2026-07-26)
- [x] Plan drafted
- [x] Plan approved (2026-07-26)
- [x] Implementation started
- [x] Phases 0–6 written to working tree and committed by user (`dbde8bc "cicd"`, 2026-07-26 22:51)
- [x] Native build (web/macos/android) verified locally (2026-07-27)
- [ ] `analyze` job's `flutter test` step gap resolved
- [ ] Docker build (linux/android) verified locally (Docker Desktop not started this session)
- [ ] Windows/iOS/docker-build.yml confirmed on real CI (not locally reproducible)
- [ ] Implementation complete

## Context Notes

Key decisions and context for resuming:

- Target: `design/vpnclient-design-prototype-v1.0.12.2025.05.02` — Flutter design prototype, web-only currently, own git repo (`git@github.com:VPNclient/vpnclient-design-prototype.git`), no `.github/workflows` yet.
- Pattern reference: `flows/sdd-comics-editor-build` — Native Build (`build.yml`, no Docker, fast CI) + Docker Build (`docker-build.yml`, Linux+Android only, reproducibility) — user asked to build the same kind of setup "аналогично sdd-comics-editor-build".
- Two key decisions already made by user (2026-07-26, via AskUserQuestion):
  1. **Docker is wanted regardless** of the simpler single-platform-web starting point, AND the user wants the Flutter project expanded back to full multi-platform (`flutter create .` for android/ios/macos/windows/linux, in addition to existing web) — significant scope addition beyond pure CI setup.
  2. **CI scope = verify + artifact only** (matches comics-editor-build: no GitHub Pages deploy, no signed release publishing).
- Key differences vs. comics-editor-build noted in requirements: no C#/.NET layer (pure Dart/Flutter, engine already mocked), one extra platform not present in the reference flow (Web) which is deliberately kept out of Docker scope (no native toolchain divergence to solve).
- Requirements approved 2026-07-26. Resolved: **agent never runs git commands in `vpnclient-design-prototype`** (hard rule, same as comics-editor-build, but here it's absolute — no commits/push at all, ever, not even with confirmation). No extra Android/Xcode SDK pinning beyond Flutter 3.44.6 — accept whatever `flutter create .` generates. Web stays Docker-free, iOS/Android unsigned, nightly cron 03:00 UTC default, `flutter_native_splash` config out of scope.
- During Specifications investigation (scratch-copy testing, not touching real repo), found a **pre-existing, unrelated blocker**: `l10n.yaml` has `synthetic-package: true`, removed entirely by current Flutter (3.44.6) — breaks `flutter analyze`/`flutter test`/builds already, before any of this flow's changes (confirmed reproduces on untouched original repo too). User decided (2026-07-26): fix as a prerequisite of this flow (added as AC #0 in requirements v1.1). Fix verified working in scratch copy: `l10n.yaml` drop `synthetic-package`, add `output-dir: lib/l10n/generated`; update 7 import lines in `lib/` from `package:flutter_gen/gen_l10n/app_localizations.dart` to `package:vpn_client/l10n/generated/app_localizations.dart`.
- Verified in scratch copy (`/private/tmp/.../scratchpad/vpnclient-proto-test`, not the real repo, deleted after use): `flutter create .` adds all 5 native platform folders cleanly without touching `lib/`/`pubspec.yaml`/`web/`; only new-file collision is boilerplate `test/widget_test.dart` (references nonexistent `MyApp` — deleted, project had no tests before). `flutter analyze`/`flutter build web --release`/`flutter build macos --release` all pass after the l10n fix. Android release APK needs no signing secrets — generated `android/app/build.gradle.kts` already defaults `release.signingConfig` to the debug config out of the box. iOS build can't be verified locally (missing iOS platform SDK component in local Xcode) — expected to work on `macos-latest` CI runner; not a code problem.
- Docker daemon not running on this machine currently — Linux/Android Docker builds not yet verified locally, planned for Implementation phase once Docker Desktop is started.
- Bundle/org ID resolved 2026-07-26: `net.nativemind.vpn.design.prototype` (final Android/iOS/macOS identifier: `net.nativemind.vpn.design.prototype.vpn_client`). Specs approved 2026-07-26.
- Plan drafted 2026-07-26: 7 phases (0: l10n fix → 1: `flutter create .` + delete boilerplate test → 2: two Dockerfiles → 3: `tool/docker-build.sh` → 4: `build.yml` 7 jobs → 5: `docker-build.yml` 2 jobs → 6: `docker/README.md` + close out flow docs). Full detail in `03-plan.md`.
- **2026-07-26 22:51, user committed everything** (`dbde8bc "cicd"`): all of Phases 1–6 (`flutter create .` scaffolding, both Dockerfiles, `tool/docker-build.sh`, `.github/workflows/build.yml`, `.github/workflows/docker-build.yml`, `docker/README.md`) landed in the real repo in one commit, including the Phase 0 l10n fix. Agent did not run this commit — user did, per the hard no-git rule. These flow docs weren't updated to reflect it until now (2026-07-27), so they lagged reality.
- **2026-07-27 10:28, user committed a new design-content import** (`2c782db "vpnclient-design-prototype-v1.0.12.2025.07.13"`) — added settings-page UI, new assets/lang files, `lib/vpn_state.dart`, `lib/localization_service.dart`. This import **reverted the Phase 0 l10n fix**: `l10n.yaml` got `synthetic-package: true` back, and all 7 files reverted to `import 'package:flutter_gen/gen_l10n/app_localizations.dart'`, which broke `flutter pub get` again (same error as the original pre-existing blocker).
- **2026-07-27, this session**: re-applied the l10n fix in the working tree (uncommitted, per the no-git rule — left for user to commit): dropped `synthetic-package: true` from `l10n.yaml` again, restored all 7 imports to `package:vpn_client/l10n/app_localizations.dart`. Then ran the Native Build workflow's steps locally: `flutter pub get` OK; `flutter analyze` — 1 pre-existing info lint only (`ClearSubscriptions` naming in `lib/mock/vpnclient_engine_mock.dart`, unrelated, exit 1); `flutter test` — **fails**, `test/` is empty (exit 1, "does not appear to contain any test files") since Task 1.2 deleted the only test file and nothing replaced it — `build.yml`'s `analyze` job runs `flutter test` unconditionally so this job would currently fail on real CI too; `flutter build web --release` OK; `flutter build macos --release` OK (`vpn_client.app`, 44.4MB); `flutter build apk --release` OK (`app-release.apk`, 51.7MB, Android toolchain present on this machine now, unlike at Specifications time); `flutter build ios --release --no-codesign` still fails locally — Xcode 26.5 app is installed but the iOS 26.5 platform *component* isn't downloaded on this machine (same category of gap as noted at Specifications time, expected to work on `macos-latest` CI which ships the full platform); `flutter build windows`/`flutter build linux` not reproducible on macOS host (unchanged, known limitation).
- **2026-07-27 (later session)**: repo directory confirmed at `design/vpnclient-design-prototype-build` (git remote `git@github.com:VPNclient/vpnclient-design-prototype.git`, branch `main`, clean before this session). Found a new commit had landed since the last session: `87b3aa0 "vpnclient-design-prototype-v1.0.12.2026.07.27+MiniVersion"` (2026-07-27 10:54, user), touching `lib/design/dimensions.dart`, `lib/pages/main/main_btn.dart`, `lib/pages/main/main_page.dart`, `lib/search_dialog.dart`. This commit **reverted the l10n fix a second time**: `l10n.yaml` had `synthetic-package: true` back, and `lib/main.dart`/`lib/search_dialog.dart`/`lib/pages/main/main_page.dart`/`lib/pages/main/location_widget.dart`/`lib/pages/apps/apps_page.dart`/`lib/pages/servers/servers_page.dart`/`lib/pages/servers/servers_list.dart` were all back to the `flutter_gen` import path — even though the previous session's fix was never committed, this content-import commit's own baseline apparently still carried the pre-fix files. Re-applied the same fix again (uncommitted). Re-ran Native Build steps: `flutter pub get` OK; `flutter analyze` — same single pre-existing `ClearSubscriptions` info lint, nothing new; `flutter test` — still fails, `test/` still empty (gap unresolved, unchanged from before); `flutter build web --release` OK; `flutter build macos --release` OK (`vpn_client.app`, 44.7MB); `flutter build apk --release` OK (`app-release.apk`, 52.1MB). Sizes grew slightly vs. the prior session, consistent with the new UI content added by `87b3aa0`. iOS/Windows/Linux not attempted again this session (same unchanged local-environment limitations as before).
- **2026-07-27 (new session)**: user asked to "reapply the fix" again and explicitly said not to run any git commands — fix files directly. New commits had landed since the previous session (up through `0920eb2 "+7"`), and `l10n.yaml`/the 7 imports were reverted again. First attempt mistakenly applied the *original spec* fix variant (`output-dir: lib/l10n/generated` + `.../generated/app_localizations.dart` import), which diverged from the actual established variant recorded in `04-implementation-log.md`'s Deviations Summary (default output dir matching `arb-dir`, import `package:vpn_client/l10n/app_localizations.dart`, no `/generated/` — chosen because the real repo has hand-checked-in generated files directly in `lib/l10n/`). Caught and corrected before finishing: removed the stray `output-dir` line and the stray `lib/l10n/generated/` folder it created, fixed all 7 imports to the correct path. `flutter pub get`/`flutter analyze` verified clean (same single pre-existing lint). No git commands run this session — working tree has the fix, nothing staged or committed.

## Fork History

- New flow (not a fork), created 2026-07-26.

## Next Actions

1. **User, urgent**: `git add`/commit the working-tree files the agent just fixed — the l10n fix (`l10n.yaml` + 7 import lines) was already confirmed committed and working on real CI (`8aac9ab2`), but the newer `flutter analyze` fix (`// ignore:` comment in `vpnclient_engine_mock.dart`) is still uncommitted. Nothing is staged or committed by the agent (git untouched per explicit instruction).
2. User: decide how to handle the `flutter test` gap in `analyze` job (add a real/placeholder test, make the step non-fatal, or drop it) — this step has not yet actually run on real CI (analyze was failing first), so it's still an open blocker for the *next* CI run once the analyze fix lands.
3. User: strongly consider a guardrail — this is now the *fifth* time a change has reintroduced `synthetic-package: true`/broken import paths. Whatever tool/process generates these content-import diffs appears to always include a stale `l10n.yaml`. Worth fixing at the source rather than continuing to catch it downstream.
4. Start Docker Desktop and verify `tool/docker-build.sh linux|android` locally (Phase 2/3 checkpoints still unverified).
5. Push and confirm windows/ios jobs and `docker-build.yml` on real CI (not locally reproducible on this machine).
